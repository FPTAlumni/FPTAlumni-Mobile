
import 'package:flutter/material.dart';

class ReferrallDetail extends StatelessWidget {
  const ReferrallDetail({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('Referral')
        ),
        body: Center(
          child: Text('Referral Detail')
        ),
    );
  }
}
